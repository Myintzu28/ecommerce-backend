spring:
  application:
    name: ecommerce
  datasource:
    url: jdbc:mysql://127.0.0.1:3305/ecommerce
    username: root
    password: bs123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080